# nhd
a single HUD: routing engines, BGP, thermals, optics, interfaces, OSPF adjacencies, the event log, all read at a glance and the *same method - SSH* across Arista, Juniper, Cisco IOS, and Linux.
